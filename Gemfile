source "https://rubygems.org"

# 核心 Jekyll 依赖
gem "jekyll", "~> 4.3"

# 常用插件（按需保留）
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

# 平台兼容配置
platforms :mingw, :x64_mingw do
  gem "tzinfo-data"
end
